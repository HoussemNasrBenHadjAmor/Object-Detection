# Base directory for images
BASE_DIR: "/kaggle/working/yolov5-peoples&cars-8"

# Ouput directory for augmentation images
OUTPUT_DIR: "/kaggle/working/yolov5-peoples&cars-8"

# Class names.
CLASSES: ["__background__", "car", "people"]

# ID classes want to augment
CLASSES_TO_AUGMENT: [1, 2, 3, 4]

CLASS_MAPPING: { 1: "car", 2: "people" }

# Output directory for augmetation images for display later
SAVE_DIR_EXAMPLES_PATH: "/kaggle/working/yolov5-peoples&cars-8/aug_examples"

# Number of augmetation per image
NUMBER_OF_AUGMETATION_PER_IMAGE: 7

# Width for image while loading the dataset
WIDTH: 1920

# Height for image while loading the dataset
HEIGHT: 1080
